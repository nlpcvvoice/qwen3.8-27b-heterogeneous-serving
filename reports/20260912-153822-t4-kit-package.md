# t4-kit 交付报告

| 项 | 值 |
|---|---|
| 日期 | 2026-09-12 15:38 |
| 目的 | 交付可移植 Kaggle T4 Job 包,供其它 agent 在其它项目目录独立使用 |
| 范围 | GPU T4(2xT4)= 全能力;**零 TPU 逻辑** |
| 位置 | `t4-kit/`(自包含)+ 报告 |
| 验证 | 离线自测 ALL PASS;`push --dry` 正常;`check` 读取真实配额 |

## 能力对照(与主仓库 T4 内核一致)

| 能力 | 交付入口 | 验证 |
|---|---|---|
| Kaggle 登录(内存 token) | `kaggle_login.py` | `check` ok=True |
| Push 2xT4 内核 | `push_job.py` / `start.sh push` | `--dry` 通过 |
| llama.cpp CUDA 源码编译(sm_75) | `kernel/serve_t4_gpu.py` | 复用已验证代码 |
| engine.tar.gz 缓存(跳过 25min 编译) | `engine_cache.py` | 离线决策模拟 |
| 事件监控 | `watch_job.py` | — |
| 远程停机(立即释放配额) | `stop_job.py` | CONTROL/stop 协议复用 |
| 输出下载 | `download_output.py` | — |
| 配额查询(仅 GPU) | `check.py` → quota_view | 真实数据 20.97h 剩 |
| 自测基准(tok/s + 4 并发) | 内核内建 | — |

## 关键设计

| 点 | 决定 |
|---|---|
| 运行时目录 | `<项目>/tmp/t4-kit/`(MASTER: tmp 归 tmp,gitignore) |
| token 路径 | env `KAGGLE_TOKEN_FILE` → kit/reference → 项目根/reference |
| 缓存格式 | `engine.tar.gz`(bin+lib),上传先验 launcher+impl.so;sha256 落 `manifest.json` |
| 缓存填仓 | `watch --stop-on built`:engine-cached → 自动 CONTROL-stop → download → upload |
| 机器档 | `NvidiaTeslaT4`(免费档,不耗捐赠币) |

## 测试记录

| 项 | 结果 |
|---|---|
| `start.sh test`(离线) | 12 项断言全部 PASS |
| `start.sh push --dry` | CFG/数据集源正确,不发起真实推送 |
| `start.sh check` | token ok · GPU 剩余 20.97h/30h · datasets 状态读取 |
| py_compile | 9 个模块全过 |

## 已知依赖

| 项 | 备注 |
|---|---|
| kaggle SDK | 需在目标 python 环境安装(kaggle>=2.1) |
| ntfy.sh | 本地到 ntfy 断连时 watch/stop 会重试;期间可用 Kaggle 网页手停 |
| 权重数据集 | 缺失时内核自动 HF 兜底下载 |