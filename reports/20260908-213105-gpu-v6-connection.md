# GPU v6 连接方式 (Qwen3.8-27B · 2×T4 · llama.cpp)

| 项 | 值 |
|---|---|
| 端点 | `https://telescope-float-donna-draft.trycloudflare.com/v1` |
| 模型 ID | `qwen3.8-27b` |
| API Key | `sk-9d68…b95de` (机密: 完整值在 `tmp/kaggle-gpu-lab.json`) |
| 内核 | `tentenshishi/qwen38-gpu-serve` (v6) |
| ntfy topic | `ktl-b3143231028a43dfa928` |
| 状态 | **SERVING/READY** · 30h keepalive |

## 一句连接法 (粘贴即用)

```bash
curl https://telescope-float-donna-draft.trycloudflare.com/v1/chat/completions \
  -H "Authorization: Bearer <KEY>" -H "Content-Type: application/json" \
  -d '{"model":"qwen3.8-27b","messages":[{"role":"user","content":"Say OK"}]}'
```

## 模型元数据 (llama.cpp /models)

| 字段 | 值 |
|---|---|
| `format` | `gguf` |
| `quantization_level` | `Q4_K - Medium` |
| `n_ctx_train` | 262144 |
| `n_ctx` (总运行ctx) | **98304** (=4槽×24576/槽) |
| `n_ctx` per-slot | 24576 (llama-server `/models` 报的槽级值) |
| `n_params` | 27,320,697,856 |
| `size` | 16,453,443,584 B (≈15.3 GiB) |
| `owned_by` | `llamacpp` |

## 并发能力 (内核启动时自测)

| 项 | 值 |
|---|---|
| 并发请求 | 4 (并行) |
| wall time | 4.3 s |
| errors | 0 |

## 已验证调用

| 检查 | 结果 |
|---|---|
| `GET /v1/models` (Bearer) | 200, 模型可见 |
| `POST /v1/chat/completions` | `OK` (54 in / 22 out tokens) |

## 冷启动链路 (v6, ~31 min 总时长)

| 起点 | +时长 | 事件 |
|---|---|---|
| Kernel 启动 | 0 | weights-mounted |
| weights-mounted | +29.0 min | tunnel-url |
| tunnel-url | +0.1 min | compiling |
| compiling | +1.6 min | serving |
| serving | +0.3 min | parallel4-test ✓ |

## 访问约束

- 免费 TPU 配额硬上限: GPU 单账户 ≤2 并发、TPU ≤1 并发(队列计入)。
- keepalive: GPU 30h / TPU 9h,超时内核自关。
- 切换控制: `app/power.py` + `app/controller.py`(live 模式, admin :8091)。
- 状态机当前: primary=**gpu**。

## 对外不可见信息

| 项 | 存储位置 |
|---|---|
| API Key 明文 | `tmp/kaggle-gpu-lab.json` |
| 内核实时事件 | ntfy topic (见上) |
| controller 状态 | `tmp/controller_state.json` |