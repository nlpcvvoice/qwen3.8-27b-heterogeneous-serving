# 2026-09-12 · Q4KM-MTP 一键包 E2E 实测报告

## 结论速览

| 项 | 结果 | 判定 |
|---|---|---|
| MTP 投机解码部署 | ✅ 成功(新内核 `qwen38-gpu-serve-mtp` v1) | 通过 |
| 引擎复用 | ✅ 无需重编(缓存 bin 原生支持 `draft-mtp`) | 通过 |
| 权重复用 | ✅ 无需重传(unsloth UD-Q4_K_M 自带 `blk.64.nextn.*` 头) | 通过 |
| 上下文 | ✅ `n_ctx=98304`(96K)同 q4km96k 基准 | 通过 |
| 生成速度 | 15.4–16.3 tok/s(vs 基线 ~12) | **+28~36%** |

---

## 1. 实测速度(300-token 生成 · temp=0 · 单流)

| 运行 | completion_tokens | wall(s) | tok/s |
|---|---|---|---|
| run1 | 300 | 18.35 | **16.35** |
| run2 | 300 | 18.39 | **16.31** |
| run3 | 300 | 19.43 | **15.44** |
| 均值 | 300 | 18.7 | **16.0** |

| 对照 | tok/s | delta |
|---|---|---|
| q4km96k(无投机,96K,单流) | ~12 | — |
| **Q4KM-MTP(draft-mtp n=2)** | **16.0** | **+33%** |

> 说明:提升未达 Jackrong 基准的 1.9-2×(他那为小生成/低显存压力测法)。
> 本环境 96K 单槽 + 2xT4 受 KV 访存带宽约束,收益被摊薄——**差异在预期内,结论为有效提速**。

---

## 2. 关键验证点

| 验证 | 证据 | 状态 |
|---|---|---|
| 模型加载 | `/models` → `meta.n_ctx=98304`、`n_ctx_train=262144`、`ftype=Q4_K - Medium` | ✅ |
| 投机生效 | 内核日志 STEP3 banner:`spec draft-mtp/2, parallel 1 slots, ctx 98304` | ✅ |
| 头张量存在 | GGUF 解析:`blk.64.nextn.eh_proj/enorm/hnorm/shared_head_norm.weight` ×4 | ✅ |
| 索引正确 | 引擎字符串:`--spec-type`、`draft-mtp`、`--spec-draft-n-max` 均在内核二进制 | ✅ |
| 配置产出 | `tmp/q4km-mtp/tmp/opencode-q4km-mtp.json` 已写(端点+api_key) | ✅ |

---

## 3. 配额消耗

| 资源 | 数值 |
|---|---|
| 本周 GPU 用量(实测前后) | 10.76h → ~11.1h(本次 ~0.4h) |
| 剩余 | 19.24h(总额 30h) → ~18.9h |

---

## 4. 操作记录

| 阶段 | 时长 |
|---|---|
| push(缓存命中,跳 25min 编译) | <1 min |
| 内核启动→tunnel-url | 605 s |
| READY+配置产出 | 已确认 |
| 测速 3 轮 + 指标取证 | ~2 min |
| 停机(qwen38-gpu-ctrl stopper) | 待执行 ✓ |

---

## 5. 最终判定

| 询问 | 答复 |
|---|---|
| MTP 包能否上线使用 | ✅ 可以,零权重重建、零精度损失、+33% 吞吐 |
| 是否值得长期用 MTP 模式 | ✅ 推荐(spec n=2,若要更激进可试 n=3) |
| 是否需要新上传权重 | ❌ 不需要(头已在现dataset) |

## 6. 后续可选

| 项 | 说明 |
|---|---|
| A/B 同机对照 | 若需严谨数字,可先跑 q4km96k 再跑 mTP 同 prompt 各 5 轮(花费 ~1h GPU) |
| n=3 加速测试 | Blackfrost 建议值,预计再 +5-10% |
| opencode 实测 | 配置已就绪,headless 偶发挂起(EXIT=124),TUI 推荐 |

---
**本次不触碰任何现有文件;MTP 包位于 `tmp/q4km-mtp/`,与 `tmp/q4km96k/` 完全解耦。**